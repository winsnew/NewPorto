import { FC } from "react";
import clsx from "clsx";
import styles from "./index.module.scss"

const Works: FC = () => {
    return (
        <section className={clsx(styles.WorkContainer)}>
            <div className={clsx(styles.WorkLayout)}>

            </div>
        </section>
    )
} 

export default Works